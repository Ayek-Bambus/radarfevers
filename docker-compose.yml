version: '3.8'

services:
  radar-server:
    build: .
    ports:
      - "3000:3000"
    restart: always

  # Nowe: Tunel Cloudflare wystawiający serwer na świat
  tunnel:
    image: cloudflare/cloudflared:latest
    command: tunnel --url http://radar-server:3000
    restart: always
